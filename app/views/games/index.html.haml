%h1 Spiele

%h3 Offene Spiele

%table.table.table-condensed.table-hover
  %thead
    %tr
      %th Name
      %th Anzahl Spieler
      %th Start
      %th Ende
      %th Spielleiter
  - @opengames.each do |game|
    %tr
      %td=link_to game.name, game
      %td=game.get_player_statistics
      %td=l game.starttime, format: :short
      %td=l game.endtime, format: :short
      %td=game.gamemaster.username
%h3 Beendete Spiele

%table.table.table-condensed.table-hover
  %thead
    %tr
      %th Name
      %th Anzahl Spieler
      %th Start
      %th Ende
      %th Spielleiter
  - @closedgames.each do |game|
    %tr
      %td=link_to game.name, game
      %td=game.get_player_statistics
      %td=l game.starttime, format: :short
      %td=l game.endtime, format: :short
      %td=game.gamemaster.username